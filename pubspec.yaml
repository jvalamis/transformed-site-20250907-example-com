name: transformed_site_20250907_example_com
description: A modern Flutter app generated from website data
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^1.1.0
  flutter_markdown: ^0.6.18
  url_launcher: ^6.2.1
  cached_network_image: ^3.3.0
  flutter_staggered_grid_view: ^0.7.0
  google_fonts: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
